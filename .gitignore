# Node
node_modules/

# Local cluster / kube state
kubeconfig
*.kubeconfig
.kube/
kind-*.yaml.local

# Secrets & credentials — never commit
.env
.env.*
*.env
vault-init.json
.vault-init.json
*-unseal-key*
*.token
root-token*
*.key
*.pem
*.crt.local
*_rsa
*_ed25519
secrets.local.*

# Local lab overrides (commit the *.example templates instead)
*.local.yaml
terraform.tfvars

# OS / editor
.DS_Store
*.swp

# Lab-generated etcd encryption key (KEK) — seed from the .example, never commit
lab-infra/encryption/encryption-config.yaml

# Domain 5 offense tooling — venv, reports, and locally cloned attack repos
lab-infra/offense/.venv-offense/
lab-infra/offense/garak_runs/
lab-infra/offense/reports/
lab-infra/offense/*.report.*
lab-infra/offense/atomic-red-team/
lab-infra/offense/caldera/
*.jwt

# Terraform (ZTNA as-code labs)
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
.terraform.lock.hcl
override.tf
override.tf.json

# Python build artifacts
__pycache__/
*.pyc
