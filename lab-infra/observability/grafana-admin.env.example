# Copy to grafana-admin.env (gitignored) and set a strong password.
# obs-dashboards: Grafana admin credential — never commit the real value.
# Consumed by up.sh to create the grafana-admin secret and set adminPassword.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-to-something-strong
