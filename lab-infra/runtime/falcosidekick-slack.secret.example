# Copy this file to  falcosidekick-slack.secret  (gitignored) and set a real webhook.
# Used by up.sh to enable the Falcosidekick Slack output (rt-response).
# Leave the file absent to run without Slack — Loki + the UI still work.
webhook=https://hooks.slack.com/services/REPLACE/WITH/YOUR-WEBHOOK
